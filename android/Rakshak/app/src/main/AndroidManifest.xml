<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ================================================= -->
    <!-- INTERNET -->
    <!-- ================================================= -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ================================================= -->
    <!-- PHONE & CALL PROTECTION -->
    <!-- ================================================= -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- ================================================= -->
    <!-- SMS PROTECTION -->
    <!-- ================================================= -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!-- ================================================= -->
    <!-- NOTIFICATIONS & OVERLAY -->
    <!-- ================================================= -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- ================================================= -->
    <!-- STORAGE (Scoped Safe) -->
    <!-- ================================================= -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- ================================================= -->
    <!-- HARDWARE -->
    <!-- ================================================= -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <!-- ================================================= -->
    <!-- PACKAGE VISIBILITY -->
    <!-- ================================================= -->
    <queries>

        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>

        <intent>
            <action android:name="android.intent.action.OPEN_DOCUMENT" />
        </intent>

        <intent>
            <action android:name="android.intent.action.OPEN_DOCUMENT_TREE" />
        </intent>

        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>

        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>

    </queries>

    <!-- ================================================= -->
    <!-- APPLICATION -->
    <!-- ================================================= -->

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rakshak"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- ================= MAIN ================= -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- ================= SECURITY DASHBOARD ================= -->
        <activity
            android:name=".ui.SecurityDashboardActivity"
            android:exported="false"
            android:label="Security Dashboard"
            android:theme="@style/Theme.Rakshak" />

        <!-- ================= RISK HISTORY ================= -->
        <activity
            android:name=".ui.RiskHistoryActivity"
            android:exported="false"
            android:label="Risk History"
            android:theme="@style/Theme.Rakshak" />

        <!-- ================= CALL PROTECTION ================= -->

        <activity
            android:name=".ui.CallProtectionSettingsActivity"
            android:exported="false"
            android:label="Call Protection Settings" />

        <activity
            android:name=".calls.ui.CallWarningActivity"
            android:exported="false"
            android:theme="@style/Theme.Rakshak" />

        <service
            android:name=".calls.RakshakCallScreeningService"
            android:permission="android.permission.BIND_SCREENING_SERVICE"
            android:exported="true">

            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>

        </service>

        <receiver
            android:name=".calls.CallWarningReceiver"
            android:exported="false">

            <intent-filter>
                <action android:name="com.rakshak.security.ACTION_SHOW_WARNING" />
            </intent-filter>

        </receiver>

        <!-- ================= SMS PROTECTION ================= -->

        <receiver
            android:name=".receivers.SmsReceiver"
            android:permission="android.permission.BROADCAST_SMS"
            android:enabled="true"
            android:exported="true">

            <intent-filter android:priority="1000">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>

        </receiver>

        <activity
            android:name=".sms.SmsWarningActivity"
            android:exported="false"
            android:theme="@style/Theme.Rakshak" />

        <!-- ================= CHATBOT ================= -->

        <activity
            android:name=".chatbot.ChatActivity"
            android:exported="false"
            android:label="Rakshak AI Assistant" />

        <!-- ================= HEALTH ================= -->

        <activity
            android:name=".health.HealthDashboardActivity"
            android:exported="false"
            android:label="Phone Health" />

        <activity
            android:name=".health.ThreatListActivity"
            android:exported="false"
            android:label="Detected Threats" />

        <!-- ================= FILE SCAN ================= -->

        <activity
            android:name=".filescanner.FileScanActivity"
            android:exported="true"
            android:label="Scan with Rakshak">

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>

        </activity>

        <activity
            android:name=".filescanner.FolderScanResultActivity"
            android:exported="false" />

        <!-- ================= PERMISSIONS ================= -->

        <activity
            android:name=".permissions.PermissionDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".permissions.PermissionEducationActivity"
            android:exported="false" />

        <!-- ================= LINK SCANNER ================= -->

        <activity
            android:name=".linkscanner.LinkDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".linkscanner.LinkScanActivity"
            android:exported="true"
            android:permission="android.permission.PROCESS_TEXT"
            android:label="Scan link with Rakshak">

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

        </activity>

        <activity
            android:name=".linkscanner.LinkResultActivity"
            android:exported="false" />

        <!-- ================= OVERLAY SERVICE ================= -->

        <service
            android:name=".ui.OverlayWarningService"
            android:exported="false"
            android:foregroundServiceType="phoneCall" />

    </application>

</manifest>
