<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ================================================= -->
    <!-- INTERNET PERMISSION (Required for AI Chatbot) -->
    <!-- ================================================= -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ================================================= -->
    <!-- PACKAGE VISIBILITY (ANDROID 11+) -->
    <!-- ================================================= -->
    <queries>

        <intent>
            <action android:name="android.intent.action.MAIN" />
        </intent>

        <intent>
            <action android:name="android.intent.action.OPEN_DOCUMENT" />
        </intent>

        <intent>
            <action android:name="android.intent.action.OPEN_DOCUMENT_TREE" />
        </intent>

        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>

        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>

    </queries>

    <!-- ================================================= -->
    <!-- HARDWARE FEATURES -->
    <!-- ================================================= -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <!-- ================================================= -->
    <!-- PHONE & SMS PERMISSIONS -->
    <!-- ================================================= -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ================================================= -->
    <!-- STORAGE PERMISSIONS (Scoped Storage Safe) -->
    <!-- ================================================= -->

    <!-- Android 10–12 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Android 13+ -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- ================================================= -->
    <!-- APPLICATION -->
    <!-- ================================================= -->

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rakshak"
        android:usesCleartextTraffic="false"
        tools:targetApi="34">

        <!-- ================= MAIN DASHBOARD ================= -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- ================= CHATBOT ================= -->
        <activity
            android:name=".chatbot.ChatActivity"
            android:exported="false"
            android:label="Rakshak AI Assistant" />

        <!-- ================= PHONE HEALTH ================= -->
        <activity
            android:name=".health.HealthDashboardActivity"
            android:exported="false"
            android:label="Phone Health" />

        <!-- ================= THREAT LIST ================= -->
        <activity
            android:name=".health.ThreatListActivity"
            android:exported="false"
            android:label="Detected Threats" />

        <!-- ================= FILE SCAN + SHARE ================= -->
        <activity
            android:name=".filescanner.FileScanActivity"
            android:exported="true"
            android:label="Scan with Rakshak">

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>

        </activity>

        <!-- ================= FILE SCAN RESULTS ================= -->
        <activity
            android:name=".filescanner.FolderScanResultActivity"
            android:exported="false" />

        <!-- ================= PERMISSION TRACKER ================= -->
        <activity
            android:name=".permissions.PermissionDashboardActivity"
            android:exported="false" />

        <activity
            android:name=".permissions.PermissionEducationActivity"
            android:exported="false" />

        <!-- ================= LINK SCANNER ================= -->
        <activity
            android:name=".linkscanner.LinkDashboardActivity"
            android:exported="false" />

        <!-- 🔗 SHARE / SELECTED TEXT → RAKSHAK -->
        <activity
            android:name=".linkscanner.LinkScanActivity"
            android:exported="true"
            android:permission="android.permission.PROCESS_TEXT"
            android:label="Scan link with Rakshak">

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

        </activity>

        <activity
            android:name=".linkscanner.LinkResultActivity"
            android:exported="false" />

        <!-- ================= RECEIVERS ================= -->
        <receiver
            android:name=".receivers.CallReceiver"
            android:enabled="true"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>

        </receiver>

        <receiver
            android:name=".receivers.SmsReceiver"
            android:permission="android.permission.BROADCAST_SMS"
            android:enabled="true"
            android:exported="true">

            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>

        </receiver>

        <!-- ================= OVERLAY SERVICE ================= -->
        <service
            android:name=".ui.OverlayWarningService"
            android:exported="false" />

    </application>

</manifest>
