<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Green background for ENABLED permission -->
    <solid android:color="#2EA043"/>

    <!-- Rounded pill shape -->
    <corners android:radius="16dp"/>

    <!-- Inner padding safety (optional but nice) -->
    <padding
        android:left="6dp"
        android:top="4dp"
        android:right="6dp"
        android:bottom="4dp"/>

</shape>
